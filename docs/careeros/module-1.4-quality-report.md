# Module 1.4 — Quality report

**Status: BLOCKED — do not use for curriculum sign-off.**

This report must document the 5-profile fixture set through extraction → O*NET mapping → embedding generation on Preview, then Production.

## Blockers

- Skill embeddings are not generated by application code yet (`user_skill_embeddings` has no write path). Complete `careeros/skills.embed` (or equivalent) before running this eval.

## Template (fill after unblock)

| User / fixture | Profile complete | Active skills ≥5 | Embeddings complete | O*NET mapped | Similarity sanity | Notes |
|----------------|------------------|------------------|---------------------|--------------|-------------------|-------|
| | | | | | | |

## Commands (Vercel-only, no localhost)

```bash
curl -s "<PREVIEW_URL>/api/careeros/_verify/skill-graph?token=$VERIFY_TOKEN&user_id=<UUID>" | jq .
```

Repeat on production after merge.
